<!--
  AndroidManifest.xml Patch
  ─────────────────────────
  Setelah `cap add android`, tambahkan atribut dan permission berikut
  ke file android/app/src/main/AndroidManifest.xml

  ① Pada tag <application>, tambahkan:
      android:networkSecurityConfig="@xml/network_security_config"

  ② Sebelum tag <application>, tambahkan permission berikut:
-->

<!-- Permission yang diperlukan Gentong Mas ERP APK -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- Feature requirements (optional) -->
<uses-feature android:name="android.hardware.camera" android:required="false" />
